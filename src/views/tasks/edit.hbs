<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
            <div class="card-body">
                <form action="/tasks/edit/{{tasks.id}}" method="POST">
                    <div class="form-group">
                        <input type="text" value="{{tasks.title}}" name="title" placeholder="Titulo" autofocus class="form-control">
                    </div>
                    <div class="form-group">
                        <input type="text" value="{{tasks.description}}" name="description" placeholder="¿Qué tienes que hacer?" class="form-control" id="">
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success btn-block">
                            <i class="fa fa-save"></i>
                            Guardar
                        </button>
                    </div>
                </form>
            </div>
            </div>
        </div>
    </div>
</div>